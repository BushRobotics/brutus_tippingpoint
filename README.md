# brutus' tipping point code

# debugging
you can test-compile by running `pros make`

you can run `pros terminal` to see the console output from `printf`

# running
`pros mu` builds & uploads to the brain
